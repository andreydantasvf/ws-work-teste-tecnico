# Como Usar os Componentes

Este guia mostra como utilizar os componentes da nossa biblioteca de UI em seus projetos.

## 🚀 Instalação e Setup

### Importando Componentes

```tsx
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
```

### Estrutura Básica

Todos os nossos componentes seguem um padrão consistente:

```tsx
<Component
  variant="default"
  size="default"
  className="custom-classes"
  {...otherProps}
>
  Conteúdo
</Component>
```

## 📋 Exemplos de Uso

### Formulário Básico

```tsx
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

function ContactForm() {
  return (
    <Card className="w-full max-w-md">
      <CardHeader>
        <CardTitle>Entre em Contato</CardTitle>
      </CardHeader>
      <CardContent className="space-y-4">
        <div className="space-y-2">
          <Label htmlFor="name">Nome</Label>
          <Input id="name" placeholder="Seu nome" />
        </div>
        <div className="space-y-2">
          <Label htmlFor="email">Email</Label>
          <Input id="email" type="email" placeholder="seu@email.com" />
        </div>
        <Button className="w-full">Enviar</Button>
      </CardContent>
    </Card>
  );
}
```

### Lista com Ações

```tsx
import { Badge } from '@/components/ui/badge';
import { Button } from '@/components/ui/button';
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger
} from '@/components/ui/dropdown-menu';

function ItemList() {
  return (
    <div className="space-y-2">
      {items.map((item) => (
        <div
          key={item.id}
          className="flex items-center justify-between p-4 border rounded"
        >
          <div>
            <h3 className="font-medium">{item.name}</h3>
            <Badge variant={item.status === 'active' ? 'default' : 'secondary'}>
              {item.status}
            </Badge>
          </div>
          <DropdownMenu>
            <DropdownMenuTrigger asChild>
              <Button variant="ghost" size="sm">
                ⋯
              </Button>
            </DropdownMenuTrigger>
            <DropdownMenuContent>
              <DropdownMenuItem>Editar</DropdownMenuItem>
              <DropdownMenuItem>Duplicar</DropdownMenuItem>
              <DropdownMenuItem className="text-destructive">
                Excluir
              </DropdownMenuItem>
            </DropdownMenuContent>
          </DropdownMenu>
        </div>
      ))}
    </div>
  );
}
```

### Modal de Confirmação

```tsx
import { ConfirmDeleteModal } from '@/components/ui/confirm-delete-modal';
import { Button } from '@/components/ui/button';
import { useState } from 'react';

function DeleteAction() {
  const [showModal, setShowModal] = useState(false);

  return (
    <>
      <Button variant="destructive" onClick={() => setShowModal(true)}>
        Excluir Item
      </Button>

      <ConfirmDeleteModal
        isOpen={showModal}
        onClose={() => setShowModal(false)}
        onConfirm={() => {
          // Lógica de exclusão aqui
          setShowModal(false);
        }}
        title="Excluir Item"
        description="Tem certeza que deseja excluir este item? Esta ação não pode ser desfeita."
      />
    </>
  );
}
```

## 🎨 Personalização

### Variantes de Cores

```tsx
// Botões
<Button variant="default">Padrão</Button>
<Button variant="destructive">Perigoso</Button>
<Button variant="outline">Contorno</Button>
<Button variant="secondary">Secundário</Button>
<Button variant="ghost">Fantasma</Button>
<Button variant="link">Link</Button>

// Badges
<Badge variant="default">Padrão</Badge>
<Badge variant="secondary">Secundário</Badge>
<Badge variant="destructive">Destrutivo</Badge>
<Badge variant="outline">Contorno</Badge>
```

### Tamanhos

```tsx
// Botões
<Button size="sm">Pequeno</Button>
<Button size="default">Padrão</Button>
<Button size="lg">Grande</Button>
<Button size="icon">🚀</Button>

// Inputs
<Input size="sm" placeholder="Pequeno" />
<Input size="default" placeholder="Padrão" />
<Input size="lg" placeholder="Grande" />
```

### Classes Personalizadas

```tsx
<Button className="bg-purple-500 hover:bg-purple-600 text-white">
  Botão Personalizado
</Button>

<Card className="border-2 border-blue-200 shadow-lg">
  <CardContent>Card personalizado</CardContent>
</Card>
```

## 🔧 Dicas e Melhores Práticas

### 1. Acessibilidade

- Sempre use `Label` com `Input` para melhor acessibilidade
- Utilize `aria-*` attributes quando necessário
- Mantenha contraste adequado nas cores

### 2. Performance

- Importe apenas os componentes que você precisa
- Use `React.memo()` para componentes que renderizam frequentemente
- Evite re-renderizações desnecessárias

### 3. Consistência

- Siga os padrões de design estabelecidos
- Use as variantes padrão quando possível
- Mantenha espaçamento consistente com classes do Tailwind

### 4. Estados de Loading

```tsx
// Botão com loading
<Button disabled={isLoading}>
  {isLoading ? 'Carregando...' : 'Salvar'}
</Button>

// Skeleton para carregamento
<Skeleton className="h-4 w-48" />
```

## 📱 Responsividade

Todos os componentes são responsivos por padrão. Use as classes do Tailwind para ajustes:

```tsx
<Button className="w-full sm:w-auto">
  Botão Responsivo
</Button>

<Card className="w-full max-w-sm md:max-w-md lg:max-w-lg">
  Card Responsivo
</Card>
```

## 🔍 Encontrando Componentes

- **Básicos**: Button, Input, Label, Badge
- **Layout**: Card, Table, Dialog
- **Navegação**: DropdownMenu, Select
- **Feedback**: Skeleton, Toaster, ConfirmDeleteModal
- **Dados**: DataTable

Explore cada componente individualmente para ver todas as opções disponíveis!

---

_Esta documentação é atualizada conforme novos componentes são adicionados._
